
1. using register var and ++i in loop:

    register int64_t sum = 0;

    for(int i=0; i<cnt; ++i)
        sum += val[i];


2.
    int a,b;
    if( (a|b)>0 )       // == if(a>0 && b>0)


3.
    for(size_t i=0, len=str.size(); i<len; ++i)


4.
    
